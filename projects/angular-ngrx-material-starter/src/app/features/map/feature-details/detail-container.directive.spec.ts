import { DetailContainerDirective } from './detail-container.directive';

describe('DetailContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailContainerDirective();
    expect(directive).toBeTruthy();
  });
});
